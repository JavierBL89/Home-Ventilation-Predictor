services:
  - type: web
    name: vent-predictor
    runtime: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: OPENWEATHER_API_KEY
        sync: false
    plan: free